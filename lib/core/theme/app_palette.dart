import 'package:flutter/material.dart';

class AppPalette {
  // Backgrounds
  static const Color background = Color(0xFF0B0F1A);
  static const Color backgroundAlt = Color(0xFF0F172A);

  // Primary gradient
  static const Color primaryA = Color(0xFF6C63FF);
  static const Color primaryB = Color(0xFF00C6FF);

  // Text
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFFA1A1AA);

  // Status
  static const Color success = Color(0xFF22C55E);
  static const Color error = Color(0xFFEF4444);

  // Surfaces / borders (dark)
  static const Color surface = Color(0xFF121A2A);
  static const Color surface2 = Color(0xFF0F1626);
  static const Color outline = Color(0xFF263248);

  // Accent glows
  static const Color glowBlue = Color(0xFF1DD3FF);
  static const Color glowPurple = Color(0xFF7C3AED);
}

